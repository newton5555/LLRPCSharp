# 项目参考资料

此目录用于放置实现和互操作验证所需的原始资料：

- `standards/`：LLRP 1.0.1、1.1、2.0、Gen2v2 标准及来源说明；
- `captures/raw/`：未经处理的 PCAP、日志和十六进制帧；
- `legacy/`：旧 SDK、生成器或示例项目的只读参考；
- `devices/`：真机型号、固件、能力和已知兼容问题记录。

标准原文、原始抓包和旧代码默认不提交 Git。可回归的数据应先脱敏、裁剪并转换到根目录 `testdata/`，同时记录来源与预期行为。

